=semantic_form_for current_user.relationships.find_by_followed_id(@user), :html => {:method => :delete} do |f|
	= f.buttons do
		=f.commit_button :button_html => {:class => "primary nice"}, :label => "Unfollow"