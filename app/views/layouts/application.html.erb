<!DOCTYPE html>
<html>
<head>
  <title>StudyAbroaders</title>
  <%= stylesheet_link_tag "application" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
<div id = "all_content">
  <%=render "layouts/above_header"%>
  <%= yield :head %> 
  <%if controller_name != 'registrations' && controller_name !="sessions"%>
  <%=render "layouts/header"%>
  <%end%>
 
  <%flash.each do |key,value|%>
    <div class="container">
      <div class="row">
        <div class="flash <%=key%> alert-box four columns"><%=value%></div>
      </div>
    </div>
  <%end%>
  <%= yield %>
  <div class="clearfooter"></div>
</div>
<div id = "custom_footer">
  <%= yield :scripts %>
  <%=render "layouts/footer"%>
</div>
</body>
</html>